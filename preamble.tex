
% Load packages
\usepackage{booktabs}
\usepackage{amsthm}
\usepackage{svg}
\usepackage[english]{babel}
\usepackage{longtable}
\usepackage{array}
\usepackage{multirow}
\usepackage{wrapfig}
\usepackage{colortbl}
\usepackage{pdflscape}
\usepackage{tabu}
\usepackage{threeparttable}
\usepackage{threeparttablex}
\usepackage[normalem]{ulem}
\usepackage{makecell}
\usepackage{xcolor}
\usepackage{hyperref}

% Set figure title above figure (default is below)
\usepackage{floatrow}
\floatsetup[figure]{capposition=top}

% Set title headings (i.e., English to Danish)
\usepackage{caption}
\captionsetup[figure]{name=Figur, labelfont=bf, textfont=it, font=small}
\captionsetup[table]{name=Tabel, labelfont=bf, textfont=it, font=small}
\usepackage{titlesec}
\titleformat{\chapter}[display] % 'display' places content on separate lines
  {\normalfont\Huge\bfseries}  % Formatting for the chapter heading
  {Kapitel \thechapter}        % "Kapitel X" on the first line
  {0pt}                        % Spacing between "Kapitel X" and the title
  {\Huge}                      % Formatting for the title
  [\vspace{-10pt}]             % Adjust vertical spacing after the title
% Reduce space between title and first line of text
\titlespacing*{\chapter}{0pt}{0pt}{10pt}

\addto\captionsenglish{\renewcommand{\contentsname}{Indholdsfortegnelse}}
\addto\captionsenglish{\renewcommand{\appendixname}{Bilag}}

% Page layout with gutter
\usepackage[a4paper, top=1in, bottom=1in, left=1.5in, right=1in, bindingoffset=0.5in]{geometry}

% Custom spacing for theorem environments
\makeatletter
\def\thm@space@setup{%
  \thm@preskip=8pt plus 2pt minus 4pt % Space before the theorem
  \thm@postskip=\thm@preskip         % Space after the theorem
}
\makeatother

% Remove gutter
% \usepackage[inner=0.5in,outer=0.5in]{geometry}

% Add title page image
\usepackage{titling}
\pretitle{\begin{center}
\includegraphics[width=5in,height=5in]{images/kap1.jpg}\LARGE\\}
\posttitle{\end{center}}

% Set Fira Sans as the main font
\usepackage{fontspec}
\setmainfont[
    BoldFont={Fira Sans SemiBold},
    ItalicFont={Fira Sans Italic},
    BoldItalicFont={Fira Sans SemiBold Italic}
]{Fira Sans}

% Set overall page format to fancy
\usepackage{fancyhdr}
\pagestyle{fancy}

% Customize header
\fancyhead[L]{\footnotesize \nouppercase{\leftmark}} % Chapter title in footnotesize
\fancyhead[R]{\footnotesize \thepage} % Page number in footnotesize

% Allows roman numbers before chap. 1 and start latin numbers from chap. 1
\pagenumbering{gobble}




